# A sample Guardfile
# More info at https://github.com/guard/guard#readme

guard 'compass' do
  watch('^sass/(.*)\.s[ac]ss')
end

guard 'compass' do
  watch('^sass/(.*)\.s[ac]ss')
end

guard 'livereload' do
  watch(%r{.+\.html})
  watch(%r{stylesheets/.+\.css})
end